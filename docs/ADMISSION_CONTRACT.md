# Admission Contract

See `V1_FOUNDATION_CONTRACT.md` (**PROPOSED**).

Eligible: supported versions + valid integrity + required fields + acceptable status.
Then Module 11 → AdmittedInput. `verified: true` alone is never enough.
